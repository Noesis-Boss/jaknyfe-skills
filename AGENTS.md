# Don Lowery's Zo Workspace

Personal Zo Computer for **jaknyfe** (Don Lowery). Use this as a routing map for settled project guidance.

## Project routing

## New project convention

- Every new project must be initialized as its own Git repository before substantive implementation begins. Keep repositories isolated; do not add a new project to an unrelated existing repository.

- **Syndicate** (`Projects/syndicate/`, dev at `http://localhost:57548`) — Existing multi-tenant SaaS agent-orchestration platform kept for reference and explicitly requested maintenance. It is not a default project focus. See `Projects/syndicate/AGENTS.md` for schema, API, and working features when this project is directly requested.
- **zo.space homepage** (`https://jaknyfe.zo.space/`) — single-page hub. Live stock/crypto tickers (SPCX, BTC, DOGE) at top, animated pegasus flying across the page, UFO visits with beam + laser every ~26s, randomly-pulsing "card ripple" shimmer on project cards, particle field, animated rings + sun behind the profile photo, 11 project cards + Web Showcase modal. Source lives in the `/` route on `jaknyfe.zo.space` (Next.js bundle). Quote data is proxied through `/api/quote` (caches Yahoo Finance for 60s).
- **Scottish Rite site** — Vite/React build deployed to `https://scottish-rite-jaknyfe.zocomputer.io/`. See `Projects/scottish-rite-site/AGENTS.md` for the build/push workflow.

## Robinhood Trading Bot

- **Path**: `robinhood-trading-bot/`
- **Purpose**: London breakout day-trading bot (from video https://youtu.be/8KblOEu56dM). yfinance data, paper-sim by default, real Robinhood via env secrets.
- **Backtest results** (2026-07-01 to 2026-08-06, 13-symbol universe, capital $10,000, theta realistic & seeded): 198 trades (99 theta spreads + 99 directional day-trades), 67.2% win rate, 1.71 profit factor, +$4,156.27 P&L. Theta contributes only +$134.02 across 99 spreads (77 wins @ +$9.29, 22 losses @ -$30 = avg +$1.35/spread); the rest (+$4,022) is equity day-trading (56.6% win rate, avg +$40.63/trade, best +$846 MARA long, worst -$416 MARA short). CLI default `--symbols` only has 5 core; pass the full 13 (SPY QQQ AAPL TSLA NVDA SOFI F AAL MARA RIVN NIO RBLX DKNG) for the full universe.
- **Theta realism fix (2026-08-08)**: theta spreads are NO LONGER booked as guaranteed full-credit wins. `_estimate_credit` now clamps premium to a realistic $5–$15/contract band and bounds spread width to a ~$20–$30 max loss; `simulate_expiry` resolves each spread by POP (80% win full credit / 20% take max loss) with `random.seed(42)`. Results: 17 win @ +$9.29 each, 7 lose @ -$30.00 each -> theta nets -$52.07 across 24 spreads (71% win rate). Prior +$4,643 / 6.26 PF / 79.2% figures were inflated by the old guaranteed-win model.
- **Win rate improvements**: volume filter, breakout-strength buffer (0.75), directional bias + trend filter, max holding bars (30), trailing stop to breakeven. +20.9% absolute win rate (58.3%→79.2%) when theta spreads included.
- **Key improvements** (vs naive box breakout): volume filter, breakout-strength buffer, directional bias + trend filter, max holding bars, trailing stop to breakeven.
- **Tuning knobs** (in `config.yaml`): `breakout_strength` (0.5–1.0), `max_holding_bars` (18–60), `rr_ratio` (1.5–3.0), `entry_window_hours`.
- **Theta farming support**: ThetaFarmer (src/theta_farming.py) sells weekly credit spreads after confirmed breakouts; Broker.place_spread() (src/broker.py); backtest.py integrates both strategies; project.py (Monte Carlo) + project_theta.py (deterministic) produce capital-aware projections saved to projections.json.
- **Capital projections** (projections.json, 20k sims, with theta farming): $100 → $1,918 median (+1,818%) at 1yr (theta disabled, below $200 threshold); $300 → $9,230 median (+2,977%) at 1yr (theta enabled at 1 contract, 7% risk/trade); $1,000 → $34,307 median (+3,331%) at 1yr (theta enabled, 5 contracts/trade). Theta threshold: ~$200 (capital-aware contract scaling: `int(risk_amount / $20 max_loss)`, no floor).

## Filesystem migration

- 2026-08-13: Moved seven isolated root artifacts to `Archive/root-artifacts/` after reference and tracking checks.
- 2026-08-13: Moved 17 clearly generated root screenshots/captures to `Media/screenshots/`; tracked root images and ambiguous source/reference media remain unmoved.
- Remaining work requires project-by-project reference maps for loose root documents, scripts, databases, duplicate project directories, and tracked/generated assets. Do not bulk-move them.

## Issue Log (2026-09-07 restore)

- 2026-09-07 (stash restore, Don-approved): Restored all 1,527 files missing from workspace using Archive/stash-recovery-2026-09-06 (stash snapshot = Sep 5 9:03pm MST, 25 min after the Sep 5 8:38pm platform restore point). rsync --ignore-existing so current (newer) files kept. Verified: 0 snapshot files still missing; only diff vs snapshot is memory/trello-progress-state.json (workspace copy newer, kept). WIP/zobodhi memory.json also skipped (current newer). Committed and pushed as 142c40834 (20,391 files) after gitleaks remediation: heartbeat.sh dropped from the commit (contained live agent key — disabled, file gitignored), 3 GitHub notification emails untracked (List-Unsubscribe tokens = false positives, gitignored). Skills/zorro, Skills/i-have-adhd, Skills/x-browser-reply NOT in snapshot, git history, tarball, or conversation workspaces — deleted before Sep 5 9:03pm; only possible source is the platform 8:38pm restore point (if deleted in the 8:38-9:03pm window) or rebuild.

## Feature Log- 2026-09-12: Next New Thing spot-check + install. Verified templetongroup/radiant is a Gatekeeper-signed macOS desktop app (SKIP stands, was never on INCLUDE). Skillspector: hindsight LOW 9/100 (pure markdown, no scripts); archify CRITICAL verdict = all false positives (env passthrough, ARCHIFY template-slot comments, test fixtures) — manually verified. Installed Skills/archify (2.17.0-dev.1, deliver smoke-tested to 819KB HTML) and Skills/hindsight. (protections)
- 2026-09-17: Expanded `Skills/gemini-video-editing/SKILL.md` with reusable 20-second vertical reel templates for talking heads, product demonstrations, and B-roll montages. Validator and full-workspace Gitleaks passed; pushed as `fb0d336e`.
- 2026-09-17: Added Gemini Omni Flash API execution via `Skills/gemini-video-editing/scripts/edit_video.py` and documented the VoiceStudio `/v1/audio/speech` narration workflow. Runner syntax and skill validation passed; VoiceStudio live test was blocked because localhost:3900 was offline.
- 2026-09-17: Registered VoiceStudio as managed service `svc_aNn9iTCplqY` with CPU-safe startup on port 3900. Service doctor reports RUNNING; v0.5.2 startup completed and OmniVoice loaded. A short CPU narration request exceeded the 30-second smoke-test window and needs a longer benchmark.
- 2026-09-10: Added tracked `bin/pre-push` secret guard and `bin/benchmark-pre-push`; the global hook delegates workspace pushes to the tracked guard. Benchmark verified the workspace path at 0.547s; pushed as 28dc1031.
- 2026-09-07: Installed deletion protections (approved by Don): (1) `bin/delete-watcher.sh` — recursive inotifywait on /home/workspace (excludes .git/node_modules/Archive/__pycache__) logging DELETE/MOVED_FROM to /var/log/workspace-deletes.log, ~60s startup walk, running as managed service `workspace-delete-watcher` (svc_xHhc6dU15X4); verified live (probe delete captured 17:26:01 UTC). (2) `bin/snapshot-loop.sh` + `bin/snapshot-workspace.sh` — nightly 03:30 UTC tar.gz of Skills/bin/memory/AGENTS.md/MEMORY.md/USER.md to Archive/snapshots/ (650MB each, 14 retained), managed service `workspace-nightly-snapshot` (svc_KLA5yMNrRwA), first snapshot verified. (3) `bin/` git-tracked and pushed to jaknyfe-skills (ef89e15a4).

## Issue Log- 2026-09-07: Trello card updates silently stopped firing — `/home/workspace/bin/` (trello-progress dedupe guard, zo-run, zo-command-audit, tailscale-curl, zo-stack-doctor) and `memory/trello-progress-state.json` had been deleted from the workspace (only surviving copy in Archive/stash-recovery-2026-09-06). Root cause of the deletion unknown. Restored all five scripts + state file from the archive, chmod +x, verified guard works (exit 0). Backfilled the missed MindCraft comment (tips page + tutorial, 2026-09-07 work) and bumped the card to top of Inbox. Also: TRELLO_API_KEY/TRELLO_TOKEN env vars return "invalid key" from the Trello REST API — card ops now go through the Pipedream trello integration (email=delowery) until the key is rotated.
- 2026-09-07 (audit follow-up): Full audit of rule-referenced paths. `bin/` was never git-tracked and no deletion commit exists — the stash snapshot proves it existed 2026-09-06 04:03 UTC and was gone by 2026-09-07; root cause of deletion remains unknown. Audit found three more missing paths: `Skills/token-saver/` (RECOVERED — re-registered with a working `scripts/run.ts` shim delegating to `/root/.token-saver/scripts/wrap.py`; install intact, verified live, pushed ece2e6bf4), `Skills/zorro/` (STILL MISSING — not in git, archive, or disk; needs rebuild if Zorro routing is still wanted), `Skills/i-have-adhd/` (STILL MISSING — behavior rules are embedded in the persona, so output style is unaffected; SKILL.md unrecoverable), and `Skills/zobodhi-memory/scripts/memory.js` (FIXED — created thin shim delegating to memory.ts, verified it reaches memory.ts gate logic). USER.md, MEMORY.md, and memory/daily/ all intact.

- 2026-08-25: Idea Desk's “Ask assistant about this idea” button was wired to a dedicated `/api/idea-desk/ask` Zo AI route. The private dashboard now opens an accessible modal with idea-specific analysis, retry handling, and contextual follow-up questions; live generation, follow-up, Escape close, and the rendered response were browser-verified in `Media/screenshots/idea-desk-assistant-response.png`.
- 2026-08-20: Idea Desk boot loop traced to a malformed `/api/idea-desk` X adapter function. Replaced the parser with syntax-safe code; API returned HTTP 200 with 22 live ideas and the dashboard screenshot rendered successfully.

## Feature Log
- 2026-09-18: Extended `Skills/agent-builder/` with an interface contract (`openai.yaml`) and reusable agent specification template. Workspace validation, quick validation, and Gitleaks passed; pushed as `4391e29d`.
- 2026-09-17: Created `Skills/gemini-video-editing/SKILL.md`, a reusable Google Gemini video-editing prompt workflow based on the supplied PDF. Validator passed, full-workspace Gitleaks scan found no leaks, and pushed as `bbbff337`.
- 2026-09-16: Humanizer gate rolled out to all copy channels. New Zo rule (id 33c1a620) requires all marketing copy through Skills/humanizer with AI-pattern score <20 before posting. Added a short-form mode (<250 words) to the SKILL.md 0-100 evaluator (length-scaled cluster thresholds + fabricated-fact override) because X-post-sized copy previously returned "insufficient text"; pushed to jaknyfe-skills (963be819). All 10 active posting automations (3 OYYB, 4 jak_nyfe, 3 zdsentry) updated with a mandatory AI-SCORE GATE section that scores final copy and logs the score. All three OYYB automations also got a HOOK SCREEN: reject hook-generator variants containing statistics, numbers, dates, or biographical claims not present in the source library (hook-generator was fabricating both stats and author biography).
- 2026-09-14: Agent Contracts follow-up added explicit Zorro tool-call records for memory, autosync, and plan operations, plus `Skills/agent-contracts/scripts/dashboard.ts` that generates a searchable HTML audit dashboard. Screenshot-verified at `runs/agent-contracts/dashboard.png`.
- 2026-09-14: Completed Agent Contracts follow-up. Zorro now records start/finish lifecycle events through `Skills/agent-contracts/scripts/agent-contract.ts`; the logger also supports read-only `list` and `show` audit views. Smoke-tested contract validation, Zorro plan execution, listing, and run detail output.
- 2026-09-14: Resumed VoiceStudio replacement evaluation. Native VoiceStudio v0.5.2 restarted on CPU at 127.0.0.1:3900; first-use model and AudioSeal assets downloaded successfully; fixed comparison sentence rendered to `Media/voice/voicestudio-comparison.wav` in 104.79s with 3.97s audio. Kokoro comparison is parked because its prior service/source is no longer present and port 3010 is unreachable.
- 2026-09-12: First archify use case: generated `robinhood-trading-bot/docs/strategy-flow.html` — showcase-validated workflow diagram of the London breakout strategy flow (Market Data → Premarket Box → Entry Window/Volume/Bias gates → Enter Trade → Manage+Journal, Theta Farmer side branch), built from real code in src/strategy.py + src/theta_farming.py. Commit 9d377bcd.
- 2026-09-07: `Skills/next-new-repo-watch` backlog fully evaluated — all 16 report stubs now have Eval + Recommendation sections (repos identified from transcripts/descriptions, include/exclude verdicts with reasons). Committed and pushed as f501e54ef. Future new videos appear as stubs; evals are agent work per SKILL.md.
- 2026-09-11: Added a post-run audit to Trello Project Progress Sync. Each run now records changed, posted, dedupe-skipped, card-created, and failed project counts; names and reasons are included for skips/failures, and posted card moves/marks are verified.
- 2026-09-12: Trello Project Progress Sync cooldown changed to 2 hours. The automation now calls `trello-progress check "<Project Name>" 2` before card actions, while preserving top-of-list moves and new-card placement.
- 2026-09-11: Trello Project Progress Sync automation was active but its instruction explicitly prohibited moving cards and omitted `pos: "top"` for new cards, so updated cards could not be promoted. Corrected the automation to move every successfully updated card to the top of its current list and create new cards at the top of Inbox; verified the @NextActions board is reachable through the delowery Trello integration and returns 124 cards.
- 2026-09-10: Meta Pixel diagnostic follow-up: live `https://noesisgroup.com/` executes `fbq`, loads `fbevents.js` with HTTP 200, and requests `signals/config` for pixel `1765605921294392`. Browser detector may be stale or privacy-blocked; source snippet is canonical and present in the live HTML.
- 2026-09-10: Added Meta Pixel PageView tracking (pixel ID 1765605921294392) to the noesisgroup.com root page; deployed with a dated server backup and screenshot-verified live.
- 2026-09-10: Fixed `Skills/x-browser-reply/scripts/follow_and_reply.py` reporting and state handling. X challenge pages are now detected, retried twice, and surfaced as an explicit error; successful replies alone mark only their own tweet IDs as replied.

- 2026-09-05: Unblocked the Skills repo master push to `Noesis-Boss/jaknyfe-skills`. The backlog of 174 unpushed commits contained >100MB blobs (moltbook_karma.log 2.5GB, paperclip-backups server.log 2.0GB + postgres dump 277MB, paperclip-restore logfile 542MB) that GitHub's pre-receive hook rejected. Stripped only those four paths from unpushed history with `git filter-repo --force --refs master --invert-paths ...` (partial mode: pushed history and working tree untouched, HEAD tree verified identical). A full push then failed mid-transfer (curl 55, pack too large) and linear chunk pushes were rejected non-fast-forward because master's history contains merged orphan lineages (site rebuilds with unrelated roots). Fix: force-pushed 25-commit topo-order chunks to a scratch `sync-staging` branch to stage objects server-side, then pushed master (d2e15f1c → faa3a99a) as a normal fast-forward and deleted the scratch branch. Master and origin are now in sync.

- 2026-08-21: Added the public Zo Space page `https://jaknyfe.zo.space/broheim` for The Original Broheim, a vintage-wanderer creative-alter-ego site with Dispatches, Artifacts, Listening Room, keyboard-operable cards, reduced-motion support, and a homepage Web Showcase link; public page screenshot and accessibility checks passed.
- 2026-08-21: Added the public Zo Space book showcase at `https://jaknyfe.zo.space/books`. It presents Outsource Your Own Brain, Bound by Ash and Thorn, and Project Orion: Alpha Protocol with 3D cover motion, controls, keyboard navigation, touch swipes, detail views, and reduced-motion handling; the rendered page was screenshot-verified.
- 2026-08-21: Replaced the temporary Bound by Ash and Thorn treatment on the public books showcase with the supplied final cover; live page screenshot verified.
- 2026-08-21: Enhanced `Skills/humanizer/SKILL.md` with a transparent 0-100 AI-pattern risk evaluator, word-count confidence rules, dimension weights, interpretation bands, and before/after reporting limits. The score is editorial guidance, not proof of AI authorship.
- 2026-09-06: Created `Skills/next-new-repo-watch/` — watches Andrew Warner's "The Next New Thing" YouTube channel (channel ID `UCNZEktrsM5oJZ-MK4jKPMOQ`), pulls transcripts + descriptions for new videos, auto-extracts GitHub repos, and generates eval report stubs in `Skills/next-new-repo-watch/reports/`. Validated live: first scan processed 15 videos and auto-extracted repos (10 from the Sep 4 Top-10 video). Pushed to Noesis-Boss/jaknyfe-skills (90fec596). Eval + recommendation step is agent work per SKILL.md.

## Issue Log (2026-09-06)

- 2026-09-06: Push to jaknyfe-skills was blocked by the gitleaks pre-push hook: 140 findings — all in a substrate-bot git stash (`refs/stash`, created 2026-09-06 04:03 UTC) that snapshotted 112,661 untracked files, including real secrets (poly-bot/.env, recovered-2026-08-27 env files). 105,159 of those stashed files no longer exist in the worktree, so the stash was NOT dropped (data-loss risk). Workaround: pushed from a clean clone at /tmp/pushclone (clones exclude stash refs; scanned clean). RESIDUAL RISK: real secrets remain inside the local stash blob of /home/workspace. Recommend Don decide: drop the stash (loses 105k untracked-file snapshot) or export/archive wanted parts first, then `git stash clear` to purge secrets from .git.
- 2026-09-06: RESOLVED — Don approved full extraction of the stash-only content. 1,535 at-risk files (384MB) extracted to `Archive/stash-recovery-2026-09-06/` (excluded: poly-bot 41 secret-bearing files, __pycache__, tracked-equivalent dupes, .githooks/.github); +2 stash-WIP tracked files (zobodhi memory.json, heartbeat.log) under `WIP/` in the same folder. Archive secret-scanned clean (gitleaks --no-git, 0 findings) and zipped to `Archive/stash-recovery-2026-09-06.tar.gz` (365MB). `git stash clear` executed — stash refs empty, full-repo gitleaks scan now reports 0 findings. Pushes from /home/workspace unblocked (verified by direct push of this commit).

## Issue Log (2026-09-06, key-leak remediation final)

- 2026-09-09: Follow-up NVIDIA `navapi`/`nvapi` audit found no matches in current workspace files or repository history. Full Gitleaks scan passed across 347 commits and ~777.8 MB. Removed invalid duplicate filename entries from `.gitleaksignore`; committed as `8d22108d` and pushed to `Noesis-Boss/jaknyfe-skills`.

- 2026-09-09: Don approved removal of `freellmapi/.env` from all `jaknyfe-skills` history. Rewrote the 355-commit local history, verified zero historical path matches, ran Gitleaks with no findings (only invalid ignore-entry warnings), and force-pushed `master`. Remote `master` now matches `79770d677`; GitHub API confirms `freellmapi/.env` is absent.

- 2026-09-06: Key-leak incident closed out. Root leak: `freellmapi/.env` + `freellmapi/server/.env` (Anthropic sk-ant, OpenAI sk-proj, OpenRouter sk-or-v1, GCP, GitHub fine-grained PAT) committed to jaknyfe-skills history and pushed to GitHub; second copy in `recovered-2026-08-27/.../bound-by-ash-technical-debt/local-live/.env`. Provider keys now disabled by user. History: remote history verified CLEAN 2026-09-06 (gitleaks 0 findings across all refs, 327 commits, 582MB; raw object grep shows only placeholder strings like `sk-or-v1-...` in docs). The earlier "dead keys remain in remote history" note was stale — force-push remediation already removed the leak commits. Local stash (substrate-bot, 140 secrets) extracted to `Archive/stash-recovery-2026-09-06/` and cleared — pushes now work directly from /home/workspace. Pre-push gitleaks hook + Zo rule enforce no-key pushes. gitleaks v8.28.0 installed at /usr/local/bin.
- 2026-09-06 (key-leak final): Leak source identified — `freellmapi/.env` + `freellmapi/server/.env` (Anthropic, OpenAI, OpenRouter, GCP, GitHub PAT) pushed in jaknyfe-skills master history (caa8e637 lineage); also recovered-2026-08-27 snapshot .env (bound-by-ash local-live), x-twitter-by-altf1be/.env, cf-tunnel-helper.html tunnel token, comment*.sh Bearer headers, heartbeat/maximize/void_daily cron keys, polymarket .env. Providers disabled affected keys; Don rotating. All branches force-pushed clean of new secrets; stash cleared after Archive/stash-recovery-2026-09-06 export; dead keys remain in remote history (documented in .gitleaksignore, purge optional). Prevention: global pre-push gitleaks hook (/root/.git-hooks/pre-push), Zo rule, and `*.env` gitignored workspace-wide. Report: /tmp/gleak/ws-full.json

## Feature Log- 2026-09-12: Weekly gitleaks sweep shipped — `bin/gitleaks-sweep.sh` (full-history gitleaks across every local git repo, JSON report to `memory/security/gitleaks-sweep-YYYY-MM-DD.json`, exit 0/1), first run verified: 18 repos, only the 3 known open-seo upstream placeholder tokens. Weekly automation created, Sundays 06:00 MST, emails delowery@gmail.com (urgent alert on new findings, clean ping otherwise); open-seo entries are pre-whitelisted. Pushed as 9192017c.
- **Zo CPU Renderer** (`Projects/zo-cpu-renderer/`) — CPU-only MuseTalk talking-face fallback for the Zo GPU worker queue. Uses local CPU PyTorch and ignored model weights; see its `AGENTS.md` and `README.md`.
