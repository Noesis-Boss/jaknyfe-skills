Write a Python file at `/home/workspace/caveman-test3/task_c.py` that:
- Has a function `word_count(text: str) -> dict` that counts word frequencies
- Case-insensitive, strips punctuation
- Returns a dict sorted by count descending
- Has a `if __name__ == "__main__":` block that tests with: "The quick brown fox jumps over the lazy dog. The dog barked at the fox!"
- Prints the word counts
