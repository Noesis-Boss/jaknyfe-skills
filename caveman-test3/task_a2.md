Write a Python file at `/home/workspace/caveman-test3/task_a2.py` that:
- Has a function `is_prime(n: int) -> bool` that checks if a number is prime
- Returns False for numbers < 2
- Uses trial division up to sqrt(n)
- Has a `if __name__ == "__main__":` block that prints all primes from 1 to 100
