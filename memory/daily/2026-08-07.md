# 2026-08-07 — ScholarSearch login/profile system

- Shipped login + profile system on ScholarSearch live site (https://scholarsearch-jaknyfe.zocomputer.io). Sign up / Sign in / Sign out (JWT localStorage), editable profile (education, state, field, interests) via `GET`/`PUT /api/profiles/me`.
- Root cause of "no login on live site": stale `dist/` — `vite build` was only transforming 4 modules from cache. Fix: `rm -rf node_modules/.vite` then full rebuild (6136 modules).
- Second gotcha: Cloudflare caches `/assets/*` for 4h; SPA-fallback had briefly served `text/html` for asset URLs, so browsers got HTML-as-JS and React never mounted (blank page). Fixed by shipping fresh asset hashes (real source changes: autocomplete attrs, scroll-behavior) — verify `Content-Type` after deploys.
- Fixed `Profile.tsx` calling nonexistent `PATCH /api/profiles/{id}` (hung on SPA fallback) → `GET`/`PUT /api/profiles/me`.
- Lesson: `bun run prod` rebuilds on every service restart; restart = fresh build. Check `curl -I <asset>` content-type after any scholarsearch deploy.

## Scholarship DB: aggregate amounts pared to annual individual values
- User flagged: large-amount scholarships showed multi-year aggregates (misleading vs annual entries).
- Fixed both DBs (main: 11,265 → 10,983; site: 11,503 → 11,183): 74 curated annual fixes + deleted ~150 junk rows (Bold.org listicles, FAQ pages, fabricated "Princeton No Essay", dup Regeneron rows).
- All $100K+ remaining = legit individual awards with explicit display labels.
- Verified live API returns corrected amounts.

## Trello sync
- 2026-08-07 17:40 MST — Posted to ScholarSearch card: login/profile system shipped to live site with cache-busting fixes (stale vite cache, Cloudflare SPA fallback, Profile.tsx endpoint), plus scholarship DB aggregate amount cleanup (main 11,265→10,983, site 11,503→10,183, 674 net reduction, ~150 junk rows deleted).
- Skipped: CoinBackers (already posted Aug 6), AZPS (trivial churn — images + demo page), MindCraft (DB file only, no new work), Robinhood (no new changes), Syndicate (no git commits in 8h window), Scottish Rite + Zo Chrome Sidebar (no recent changes).

## addyosmani/agent-skills installed + wired into Zorro
- 9 skills copied to /home/workspace/Skills/: using-agent-skills (discovery router), spec-driven-development, planning-and-task-breakdown, incremental-implementation, code-review-and-quality, test-driven-development, security-and-hardening, doubt-driven-development, frontend-ui-engineering. Each skill has its referenced references/*.md bundled locally; shared agent-skills-references dir removed.
- Zorro wiring in Skills/zorro/scripts/zorro.ts: new AGENT_SKILLS boost list (+0.3 score) and using-agent-skills + doubt-driven-development added to EXEC_METHOD_SKILLS (always-injected methodology).
- Verified: `bun run zorro.ts skills "hardening and security review"` → security-and-hardening top hit; "break down feature" → planning-and-task-breakdown top hit.
- Repo: 81k stars, last commit 2026-08-06, 24 skills + agents/commands/hooks/evals (not installed).
