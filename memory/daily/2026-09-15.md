
- 2026-09-15 23:35 MST — CPU fallback renderer: created `Projects/zo-cpu-renderer` as an isolated Git repo; installed CPU-only PyTorch 2.14.0, pinned MuseTalk CPU port and local weights; fixed image-mode cleanup; verified a 2-second H.264/AAC render in ~60 seconds with visual frame inspection. Registered managed service `zo-cpu-render-worker` (`svc_St74rJQmbYo`) as `cpu-zo-fallback`; queue delays CPU claims 90 seconds and excludes maintenance jobs. Shared worker token fallback reads `/root/.zo_secrets` when service env contains a literal placeholder.
- 2026-09-16 06:10 MST — Next-step benchmark: 10-second real Don headshot plus narration rendered CPU-only in 334 seconds; output `cpu-benchmark/don-headshot-10s.mp4` was H.264/AAC 1024x1272 and a frame passed visual inspection. First rerun exposed a non-idempotent patch/IndentationError; `prepare-upstream.sh` was corrected and now applies twice cleanly.
