---
date: 2026-06-12
type: daily
---

# 2026-06-12

## Meerkat MCP evaluation — paused pending vendor fix

**Goal:** Evaluate getmeerkat.dev's MCP server for use as a prompt-engineering tool inside Zo.

**What worked:**
- Authenticated as `delowery@gmail.com` via Supabase PKCE OAuth.
- Captured 5 MCP tool list: `list_projects`, `search_shelf`, `add_to_shelf`, `refactor_prompt`, `chat_with_meerkat`.
- Captured all 17 internal web API routes (including `/api/roast`, `/api/custom-chat`, `/api/head-to-head`, `/api/share`, `/api/auto-file`).
- `/api/roast` works end-to-end (3-frontier-model critique, no paywall, structured output with `burns` array and 6-digit share token).
- Built `/tmp/meerkat_api.py` client with the session cookies.

**What didn't:**
- `/api/custom-chat` returns HTTP 500 for every model slug (`mt-balanced`, `mt-fastest`, `mt-professional`, `mt-imaginative`) and every payload shape. Server-side bug — server is calling `.map` on undefined.
- Each 500 still decrements `chatTurns` quota. 6 turns used during diagnosis, all failing.
- Meerkat's MCP server returns a paywall for non-browser use unless you have a $9/mo Pro account.
- OAuth access token expires in 1 hour. Refresh path is not exposed.

**Decision:** Pause. Don't burn the 40/month promptRuns quota on a broken endpoint.

**Next steps when picking this up:**
1. Check getmeerkat.dev status page or Schmade's X account for the `custom_chat` outage.
2. If they fix it, retest with a single minimal prompt to confirm.
3. If they don't fix in 1-2 weeks, drop the project — the value proposition was the three-model synthesis, and `roast` alone doesn't justify a skill.
4. If keeping: build a `Skills/meerkat-mcp/` skill modeled on `Skills/hook-generator/` (frontmatter + scripts/meerkat.py). Pre-pend session-refresh logic (handle 401 by re-running PKCE). Wrap the paywalled MCP and the working web APIs behind a single interface.

**Files saved:**
- `file 'memory/projects/meerkat-mcp.md'` — full project note (auth flow, API list, slug list, blocker detail)
- `/tmp/meerkat_api.py` — working session-cookie API client (transient, but the schema is in the project note)
- `/home/.z/workspaces/con_gm2sF13lbO4c5RUt/meerkat-cookies.json` — current session cookies (3.4KB; will go stale in ~1 hour)

**Scripts/automations touched:** none. Nothing to revert.
