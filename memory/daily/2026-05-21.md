---
name: 2026-05-21-daily
description: Clarion memory system installed; zobodhi-memory wired as complementary fast-query layer
type: daily
---

# 2026-05-21 Daily Note

## What We Worked On

- Evaluated Clarion Intelligence Systems' "Memory System Quick Start" page
- Compared Clarion's architecture to the existing `zobodhi-memory` skill
- Installed Clarion's full memory system alongside `zobodhi-memory`
- Configured the two systems to be complementary (Clarion as primary structured layer, zobodhi-memory as fast-query cache)
- Created rules for automatic context loading and proactive capture

## Decisions Made

- Clarion is the **primary memory system**: structured files, taxonomy, auto context loading via rules
- `zobodhi-memory` is the **fast-query layer**: flat JSON cache for quick substring searches, synced on key facts
- Dual-layer design: Clarion manages depth + structure; zobodhi-memory handles speed + cross-project lookups
- Created `memory/SYSTEM_CONFIG.md` as Don's owned edit file (pending-change queue)
- Registered two rules: (1) context load at start, (2) active capture + zobodhi-memory sync

## Next Steps

- Populate `memory/projects/` entries for each active project (Scottish Rite, ScholarSearch, Paperclip, Kilo, greeting-gallery)
- Add `memory/feedback/` entries for known preferences (short answers, screenshot verification, exact format for reports)
- Seed `zobodhi-memory` with key facts from Don's profile and active projects
- Review and prune `SYSTEM_CONFIG.md` pending changes section as needed

## Notes

- `zobodhi-memory` lives at `Skills/zobodhi-memory/` — it is a separate, self-contained skill
- Clarion system lives at `/home/workspace/memory/` — primary structured memory
- `USER.md` and `MEMORY.md` are at `/home/workspace/` root — first-class citizens in the memory system
- The dual-layer approach means Don can use `memory.ts --query` for fast lookups without touching the structured folder system