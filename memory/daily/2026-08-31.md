- 2026-08-31 (Momball): Planning decision — main-page photo uploads go to an admin review queue before publishing into a public rotation; gallery shows approved community pool, not the submitter's own device-local photos. Docs updated in Projects/momball (commit 2b6ffd0).
- 2026-08-31: Momball photo-moderation decision: main-page photo uploads must go to an admin review queue; approved photos publish into a public rotation shown to all visitors — never device-local-only display. Captured in momball planning docs (commit 2b6ffd0). Site live at https://jaknyfe.zo.space/momball (public).

- Momball photo moderation requirement captured (commit 2b6ffd0): main-page photo uploads go to an admin review queue; approved photos publish into a public rotation shown identically to all visitors — displayed photos are never just the submitter's own device-local pictures. Current device-local Gallery is explicitly interim. Trello Momball card updated.
- Published Momball as a public Zo Site: https://momball-site-jaknyfe.zocomputer.io (scaffold at Projects/momball-site, commit 75bf3ae). Live render screenshot-verified. Next: photo review queue + public approved-photo rotation.

## SaaS-Mailer — dashboard, enroll flow, sending-account connect (2026-08-31)

- Completed PostgreSQL cutover: worker service RUNNING, CREDENTIAL_ENCRYPTION_KEY verified (64-char hex), `start:worker` script restored; prod service command updated via update_user_service.
- Dashboard rebuilt: five real sections wired to new `GET /api/contacts` + `GET /api/campaigns`; campaigns refresh after create/approve/enroll; contact-row and record CSS fixed; cache-bust `v=20260831-1`.
- Shipped enroll-into-campaign action (idempotent, INSERT OR IGNORE) and sending-account connect UI (Gmail/Microsoft OAuth 302-to-consent verified; mock test-account connect form).
- 32/32 tests pass; commits 051aefd, 9cc7438, d205ba1, 83aa463, 584f508, b7e16b7, 1576f86. Every increment screenshot-verified on dev and the live production site.
- Remaining next: campaign steps editor beyond step 1. Ops note: bun test hangs if dev servers hold the SQLite DB lock — kill dev servers by listening PID before running the suite.
