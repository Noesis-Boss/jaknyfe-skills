# 2026-08-05

## Story-arc candidates (scatterbrain rerun, ATC frame, reasoning-only)

6 ideas generated after user rejected both earlier scatterbrain batches (Echo Market set; Wreckage Oracle set). Sandbox/zombie child sessions kept timing out, so generated from reasoning under the ATC frame.

1. The Odd Hours — hospice 3am mysteries (holding)
2. Paper Ocean — ghost freighter reclaims its pilot (handoff)
3. The Quiet Comms Act — AM granny becomes ATC for the midwest (flows)
4. Skylark Fourteen — 14 extra minutes of impossible cockpit audio (altitude/authority)
5. The Holding Fix — dead circle town as weather; meteorologist routes souls (literal holding patterns)
6. Tower 6 — six controllers die in same chair preventing worse crashes (cross-time handoff)

- Strongest legs: The Quiet Comms Act
- Highest spark: The Holding Fix
- Sleeper: Paper Ocean

## MindCraft — magic-link auth + verified practice loop

- Built account system: magic-link email login (POST /api/auth/request, GET /api/auth/verify, me/logout), users+sessions persisted in `Projects/MindCraft/data/auth-db.json` (sha256 token hashes), 30-day sessions, 15-min link expiry.
- Frontend: `src/lib/auth-context.tsx` (session in localStorage, token pickup from #/auth/session), `src/pages/login.tsx` (dev-mode inline link when no RESEND_API_KEY), `UserMenu` dropdown w/ logout.
- Practice flow verified end-to-end in browser as delowery@gmail.com: add text → 3 cycles (full text → first letter+length → length only, single toggle button) → rate Good → SM-2 card persisted (state=learning, reps=1, ease=2.5, due set).
- RESEND_API_KEY not set — email not sent; dev link shown inline. Set secret + verify `onboarding@mindcraft.zocomputer.io` in Resend before shipping.
- Committed `b1de639` to MindCraft repo. Trello @NextActions: progress comment posted on MindCraft card.

## MindCraft — server-side per-user text storage (2026-08-05)

- Rewired library from localStorage to a SQLite backend: `data/mindcraft.db` (tables `texts`, `chunks`).
- New REST API mounted at `/api/texts` in `server.ts`: GET list, POST create (auto-chunked), DELETE, POST /:id/rate (SM-2 rating persisted).
- All endpoints session-gated via `x-session` magic-link token; data scoped by `user_email` — two users verified isolated.
- Client calls through `src/lib/texts-client.ts` (same SESSION_KEY as auth-context). App state still in React (library list), source of truth is SQLite.
- Verified build + restart + HTTP round-trip; user A text invisible to user B; rating persisted across restart.
