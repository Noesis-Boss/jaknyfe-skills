## 2026-08-08 — ScholarSearch fix (blank page / no login / no previews)

- Symptom: site showed only "5,000+ Scholarships", no scholarship cards, no login.
- Root cause A: `getStatGroups()` crashed on NULL category row (381 rows) → React unmounted whole app → blank. Fixed server.ts getCategories() with COALESCE → 'Uncategorized'.
- Root cause B: Home.tsx called PHP-era `/api.php/api/*` paths; Bun server has `/api/*` only. Changed to `/api/scholarships` + `/api/stats`.
- Hardened: all localStorage access wrapped (try/catch + memory fallback) — sandboxed browsers block storage.
- Verified live: stats 10,747 real, 12 scholarship cards render, Sign in/Sign up nav, login→register→profile save works.
- Deploy: build + service restart (svc__BI1M5KYZBE, https://scholarsearch-jaknyfe.zocomputer.io).

## 2026-08-08 — ScholarSearch live-site fixes (blank page / no login / no previews)
- Root cause 1: `/api/stats` returned `category: null` rows (381); Home.tsx `category.toLowerCase()` threw → React unmounted entire app → blank page, no login nav.
- Root cause 2: Home/Admin still called PHP-era `/api.php/api/...` paths that don't exist on the Bun server → stats stuck at "5,000+" fallback, zero results.
- Fixes: COALESCE null categories in server.ts; switched frontend to `/api/*`; wrapped all localStorage in safe helpers. Rebuilt + restarted service svc__BI1M5KYZBE.
- Verified live via Zo browser: 10,747 total, 12 scholarship cards, Sign in/Sign up nav, login+profile save flow tested locally. Trello ScholarSearch card commented.
# 2026-08-08

- ScholarSearch: fixed blank homepage / "5,000+ only" / missing login. Root causes: (1) Home.tsx called `/api.php/api/...` (server has `/api/*`), (2) `/api/stats` had a NULL category row → `category.toLowerCase()` crashed React render → whole app blank. Fixed paths, added null guard + `COALESCE(category,'Uncategorized')` in server, hardened localStorage. Verified live: Sign in/Sign up nav, 10,747 total, 12 scholarship cards, login→profile save works. Service `svc__BI1M5KYZBE` restarted, new hash index-Dvmgme7V.js.

## 2026-08-08 17:15 MST — Trello sync
- Posted progress comment to Robinhood Trading Bot card: theta realism fix (premium clamping, POP-based expiry, 11 source files updated). No other projects had significant changes since last sync (ScholarSearch already posted, AZPS trivial churn, Syndicate no recent commits).
