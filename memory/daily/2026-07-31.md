# Daily Notes — 2026-07-31

## token-saver evaluation for Zo
## Zorro presentation + Zero-Cost Zo course

- Created `Zorro-Presentation/` with three deliverables (all verified live 2026-07-31):
  - `01-presentation-outline.md` — 15-slide deck on the Zorro persona stack: memory, token discipline, free LLMs, verification culture, self-improvement loop
  - `02-course-outline.md` — "Zero-Cost Zo" course: 6 modules (orientation, 3-layer memory, token discipline, free LLMs, verification, capstone build-your-own-Zorro), each with labs + success checks + troubleshooting
  - `zorro-stack-architecture.d2` — architecture diagram source (PNG render blocked: d2/Playwright CDN 404 in sandbox; renders on networked machines)
- Verified facts baked into the materials: AstraDB 67 memories across 7 sources (fact 38 / session 15 / semantic 14); local nomic-embed-text-v1.5 (768-dim) embeddings; token-saver 96.8% reduction on git log --stat; Ollama gemma3:1b/4b live via OpenAI-compatible proxy on :11435 (curl 200 OK)
- Zorro persona runs on custom BYOK endpoint (byok:ee9b6e08-3859-4d08-91ec-bfc683010ef4)
- Container IP check today: `hostname -I` → 172.20.10.117 (AGENTS.md still lists 172.20.33.184 — verify before use)

- token-saver v2.6.3 (https://github.com/ppgranger/token-saver) is a content-aware CLI output compressor with 32 specialized processors (git, docker, npm/yarn/pnpm/bun, cargo, pytest/jest, eslint/ruff, gh, kubectl, terraform, helm, go, python/pip, etc.)
- Installed successfully via `python3 install.py --target claude`. Files at `/root/.claude/plugins/cache/token-saver-marketplace/token-saver/2.6.3/`, CLI at `/root/.local/bin/token-saver`
- **Zo does NOT process PreToolUse Bash hooks** — confirmed by test: added a Bash PreToolUse hook to `~/.claude/settings.json`, ran a command, hook did not fire. Zo has its own bash tool separate from Claude Code's hook system.
- `CLAUDE_PROJECT_DIR` and `CLAUDE_PLUGIN_ROOT` are NOT set in Zo's environment (OpenWolf hooks are dormant since April 2026).
- **Direct CLI usage works**: `python3 /root/.token-saver/scripts/wrap.py '<command>'` or `/root/.local/bin/token-saver benchmark '<command>'`
- **Compression results**: 80-97% token savings on verbose commands (e.g., `git log --oneline -50 --stat`: 4,097→131 tokens, 96.8% savings)
- **Recommendation**: Package as a Zo skill for manual invocation. Automatic hook integration is not possible in current Zo.
- **DONE**: Skill wrapper at `Skills/token-saver/scripts/run.ts` (version/explain/benchmark/stats/wrap subcommands) verified working — `git log --oneline -50 --stat`: 4,097→131 tokens (96.8%). `ts` alias added to `~/.bashrc`.

## install-zorro skill built (2026-07-31)

- Created `Skills/install-zorro/` — one-shot installer replicating the Zorro stack on a fresh Zo Computer.
- Script: `scripts/install-zorro.sh` (bash, idempotent, FORCE/SKIP_* flags, ZORRO_HOME override).
- Installs: memory tree + rules (USER.md/MEMORY.md/AGENTS.md), zorro, astra-memory, zobodhi-memory, ollama-proxy, token-saver skills (bundled assets), obra/superpowers skills (git clone), @xenova/transformers (local nomic embeddings), Ollama + gemma3:1b/4b, token-saver CLI (clone + install.py + `ts` alias).
- UI-only steps documented in `references/MANUAL-STEPS.md` (persona paste from `references/ZORRO-PERSONA-PROMPT.md`, Ollama provider Base URL http://localhost:11435/v1 key "ollama" model gemma3:1b, AstraDB secrets ASTRA_DB_ENDPOINT + ASTRA_DB_APPLICATION_TOKEN, 2 service registrations from `references/SERVICES-REQUEST.md`).
- Verified: fresh install + idempotent re-run both exit 0 (ZORRO_HOME=/tmp/ztest SKIP_OLLAMA=1); bundled skills byte-identical to live copies.
