# Task 4: Fibonacci

Write a Python file at `/home/workspace/caveman-test2/fibonacci.py` that:
- Has a function `fibonacci(n: int) -> int` that returns the nth Fibonacci number (0-indexed: fib(0)=0, fib(1)=1)
- Uses an iterative approach (not recursive)
- Has a `if __name__ == "__main__":` block that prints the first 20 Fibonacci numbers
