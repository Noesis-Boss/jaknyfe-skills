# Agent Profile: ScholarSearch Site

- Retrieval: Graphify first for code relationships.
- Commands: use `zo-run` for tests, builds, and reports.
- Safety: never mutate scholarship databases without validated backup, lock, transaction, and rollback guards.
- Verification: confirm direct application URLs, synchronized counts, and SQLite integrity.
- Output: include accepted, rejected, duplicate, and validated totals.
