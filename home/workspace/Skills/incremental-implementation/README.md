# incremental-implementation

**Folded into [`spec-driven-development`](../spec-driven-development/).** This skill's responsibilities (execute tasks one at a time, log proof, respect the Definition of Done) are now Stage 5 of the spec-driven-dev pipeline.

Use `spec-driven-development` instead. The single canonical `references/definition-of-done.md` lives there.

Kept as a folder only for backward-compatible imports.
