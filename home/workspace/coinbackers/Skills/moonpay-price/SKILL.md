---
name: moonpay-price
description: Retrieve live USD price for any supported cryptocurrency via Moonpay API.
---

This skill provides a tiny CLI script `price.ts` that accepts a cryptocurrency symbol (e.g., `eth`, `btc`, `usdc`) and returns the current USD price as JSON.
