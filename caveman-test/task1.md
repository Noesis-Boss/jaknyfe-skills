# Task 1: FizzBuzz

Write a Python file at `/home/workspace/caveman-test/fizzbuzz.py` that:
- Implements fizzbuzz for numbers 1 to 100
- Prints each number, but for multiples of 3 prints "Fizz", for multiples of 5 prints "Buzz", for multiples of both prints "FizzBuzz"
- Uses a function `fizzbuzz(n)` that returns the string
- Has a `if __name__ == "__main__":` block that runs it for 1-100
