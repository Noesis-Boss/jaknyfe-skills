# Task 2: Palindrome Checker

Write a Python file at `/home/workspace/caveman-test/palindrome.py` that:
- Has a function `is_palindrome(s: str) -> bool` that checks if a string is a palindrome
- Ignores case, spaces, and punctuation (only consider alphanumeric chars)
- Has a `if __name__ == "__main__":` block that tests it with at least 5 test cases including: "racecar", "A man a plan a canal Panama", "hello", "Was it a car or a cat I saw?", "noon"
- Prints each test case and result
